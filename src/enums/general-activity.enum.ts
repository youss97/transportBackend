export enum GeneralActivityType {
  ATTENDANCE = 'ATTENDANCE', // Pour pointage (début, fin, pause, etc.)
  LOADING = 'LOADING',       // Pour chargement
  OTHER = 'OTHER',           // Pour le reste
}
